# CLAUDE.md

This repository's assistant instructions live in [`.github/copilot-instructions.md`](./.github/copilot-instructions.md).

That file is the single source of truth — it links to the scoped instruction sets under `.github/instructions/` (one for `client/**`, one for `server/**`). Read it first, then follow the scoped file whose `applyTo` glob matches the path you are editing.
